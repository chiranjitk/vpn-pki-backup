'use client'

import { AppLayout } from '@/components/layout/app-layout'
import { RoutesContent } from '@/components/network/routes-content'

export default function RoutesPage() {
  return (
    <AppLayout>
      <RoutesContent />
    </AppLayout>
  )
}
